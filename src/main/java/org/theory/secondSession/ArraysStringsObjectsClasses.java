/*
 * =====================================================
 *
 *
 * 1️⃣ Как работают массивы (одномерные)
 * 2️⃣ Работа со строками:
 *    - String
 *    - StringBuilder
 *    - StringBuffer
 * 3️⃣ Что такое класс, объект и наследование
 * 4️⃣ Методы:
 *    - создание
 *    - параметры
 *    - return
 *    - перегрузка
 *    - переопределение
 *
 * =====================================================
 */

public class ArraysStringsObjectsClasses {

    public static void main(String[] args) {

        /*
         * =====================================================
         * 1️⃣ МАССИВЫ В JAVA
         * =====================================================
         */

        /*
         * МАССИВ — это:
         * - ссылочный тип данных
         * - хранит несколько значений ОДНОГО типа
         * - имеет ФИКСИРОВАННЫЙ размер
         *
         * В памяти:
         * переменная массива хранит ССЫЛКУ на область в heap
         */

        int[] numbers = new int[5];
        // Создали массив из 5 элементов
        // По умолчанию все элементы = 0

        /*
         * Индексы массива:
         * numbers[0]
         * numbers[1]
         * numbers[2]
         * numbers[3]
         * numbers[4]
         */

        numbers[0] = 10;
        numbers[1] = 20;
        numbers[2] = 30;

        System.out.println("Массив numbers:");
        for (int i = 0; i < numbers.length; i++) {
            System.out.println("numbers[" + i + "] = " + numbers[i]);
        }

        /*
         * ВАЖНО:
         * numbers.length — это размер массива
         * Последний индекс всегда = length - 1
         */

        /*
         * Короткая инициализация массива
         */
        int[] numbers2 = {1, 2, 3, 4, 5};

        /*
         * КОПИРОВАНИЕ МАССИВА
         * На самом деле копируется ССЫЛКА, а не данные
         */
        int[] ref = numbers2;
        ref[0] = 999;

        System.out.println("\nПосле изменения ref:");
        System.out.println("numbers2[0] = " + numbers2[0]);

        /*
         * Вывод:
         * numbers2 и ref указывают на ОДИН и тот же массив
         */


        /*
         * =====================================================
         * 2️⃣ СТРОКИ В JAVA
         * =====================================================
         */

        /*
         * String — это класс
         * Особенность: ОБЪЕКТЫ String НЕИЗМЕНЯЕМЫ (immutable)
         */

        String s1 = "Hello";
        String s2 = s1;

        /*
         * s1 и s2 указывают на один объект "Hello"
         */

        s2 = s2 + " World";

        /*
         * Здесь:
         * - создаётся НОВЫЙ объект "Hello World"
         * - s2 начинает ссылаться на него
         * - s1 остаётся ссылаться на "Hello"
         */

        System.out.println("\nString:");
        System.out.println("s1 = " + s1);
        System.out.println("s2 = " + s2);

        /*
         * Именно поэтому String безопасен,
         * но неэффективен при частых изменениях
         */

        /*
         * StringBuilder
         * - изменяемый (mutable)
         * - быстрее
         * - НЕ потокобезопасный
         */

        StringBuilder builder = new StringBuilder("Hello");

        builder.append(" ");
        builder.append("Java");
        builder.append("!");

        System.out.println("\nStringBuilder:");
        System.out.println(builder.toString());

        /*
         * StringBuffer
         * - почти то же самое, что StringBuilder
         * - НО потокобезопасный
         * - работает медленнее
         */

        StringBuffer buffer = new StringBuffer("Hi");
        buffer.append(" World");

        System.out.println("\nStringBuffer:");
        System.out.println(buffer);

        /*
         * Когда использовать:
         * String → обычный текст
         * StringBuilder → циклы, сбор строк
         * StringBuffer → многопоточность
         */


        /*
         * =====================================================
         * 3️⃣ КЛАСС И ОБЪЕКТ
         * =====================================================
         */

        /*
         * КЛАСС — это чертёж, описание
         * ОБЪЕКТ — это экземпляр класса
         */

        Person person = new Person("Ivan", 25);
        person.sayHello();

        /*
         * =====================================================
         * НАСЛЕДОВАНИЕ
         * =====================================================
         */

        /*
         * Student — это Person
         * Student наследует поля и методы Person
         */

        Student student = new Student("Anna", 20, "Java");
        student.sayHello(); // переопределённый метод
        student.study();


        /*
         * =====================================================
         * 4️⃣ МЕТОДЫ
         * =====================================================
         */

        /*
         * Метод — это:
         * - блок кода
         * - который можно вызывать
         * - может принимать параметры
         * - может возвращать результат
         */

        int result = add(5, 10);
        System.out.println("\nРезультат сложения = " + result);

        /*
         * ПЕРЕГРУЗКА МЕТОДОВ
         * - одинаковое имя
         * - разные параметры
         */

        double doubleResult = add(2.5, 3.5);
        System.out.println("Результат double = " + doubleResult);

        printMessage("Java — это не страшно 🙂");
    }

    /*
     * =====================================================
     * МЕТОДЫ
     * =====================================================
     */

    /*
     * static — метод принадлежит классу
     * int — тип возвращаемого значения
     */
    static int add(int a, int b) {
        return a + b;
    }

    /*
     * Перегруженный метод
     */
    static double add(double a, double b) {
        return a + b;
    }

    /*
     * void — метод ничего не возвращает
     */
    static void printMessage(String message) {
        System.out.println(message);
    }
}

/*
 * =====================================================
 * КЛАСС Person
 * =====================================================
 */

class Person {

    /*
     * Поля класса (состояние объекта)
     */
    String name;
    int age;

    /*
     * Конструктор
     * Вызывается при создании объекта
     */
    Person(String name, int age) {
        this.name = name; // this — ссылка на текущий объект
        this.age = age;
    }

    /*
     * Метод класса
     */
    void sayHello() {
        System.out.println(
                "\nПривет, меня зовут " + name +
                        ", мне " + age + " лет"
        );
    }
}

/*
 * =====================================================
 * НАСЛЕДОВАНИЕ: Student extends Person
 * =====================================================
 */

class Student extends Person {

    String subject;

    Student(String name, int age, String subject) {
        super(name, age); // вызов конструктора родителя
        this.subject = subject;
    }

    /*
     * ПЕРЕОПРЕДЕЛЕНИЕ МЕТОДА
     * - такое же имя
     * - такие же параметры
     * - другая реализация
     */
    @Override
    void sayHello() {
        System.out.println(
                "\nЯ студент. Меня зовут " + name +
                        ", я изучаю " + subject
        );
    }

    void study() {
        System.out.println(name + " учит " + subject);
    }
}

